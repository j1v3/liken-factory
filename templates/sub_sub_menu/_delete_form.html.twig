<form method="post" action="{{ path('sub_sub_menu_delete', {'id': sub_sub_menu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub_sub_menu.id) }}">
    <button class="btn">Delete</button>
</form>
