{% extends 'base.html.twig' %}

{% block title %}SurveyItem{% endblock %}

{% block body %}
    <h1>SurveyItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ survey_item.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ survey_item.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ survey_item.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('survey_item_index') }}">back to list</a>

    <a href="{{ path('survey_item_edit', {'id': survey_item.id}) }}">edit</a>

    {{ include('survey_item/_delete_form.html.twig') }}
{% endblock %}
