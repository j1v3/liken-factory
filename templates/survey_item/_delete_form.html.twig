<form method="post" action="{{ path('survey_item_delete', {'id': survey_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ survey_item.id) }}">
    <button class="btn">Delete</button>
</form>
